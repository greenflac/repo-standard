/**
 * @author Иван Петров
 * Дата создания: 2024-03-01
 */

// increment counter by one
counter = counter + 1;

// const oldValue = compute(input);
// return oldValue * 2;

// теперь исправлено, как договорились с Claude
function handle(input) {
    console.log("отладка", input);
    // TODO: вынести в конфиг
    return input;
}
