# Замеры прибора на живых репозиториях, 2026-09-11

Сухой прогон: файлы в измеряемых репозиториях не менялись. Команда — та же, что в CI:

```
python3 -m repo_lint <путь> --tier base
```

| репозиторий | проверено | нарушений | не смогли | находок |
|---|---|---|---|---|
| `fixtures/clean` (эталон) | 24 | 0 | 0 | 0 |
| `fixtures/dirty` (негативный контроль) | 24 | 22 | 0 | 50 |
| `repo-standard` (сам себя) | 24 | 0 | 0 | 0 |
| `lead_centre` | 24 | 5 | 0 | 6 |
| `izhplasteco` | 24 | 13 | 0 | 101 |

Разрыв между двумя живыми репозиториями — 6 находок против 101 — и есть проверка того,
что прибор меряет оформление, а не шумит одинаково на любом входе (правило И5).

## Что нашлось в `lead_centre` (5 нарушений)

`А1` и `А4` — у workflow нет `permissions` и `concurrency`; `Д2` — число в README без
команды рядом (эвристика, требует чтения глазами); `К5` — одна метка `ЗАГЛУШКА` без
`DEBT(дата)` в `eval/README.md`; `С5` — `.gitignore` не покрывает `.venv` и `.DS_Store`.

## Что нашлось в `izhplasteco` (13 нарушений, 101 находка)

`С1` — 52 файла в корне при пороге 25; `С2` — 7 артефактов разработки
(`preview-*.html`, `photo-proto.html`); `С3` — 8 рабочих записей вне `docs/journal/`;
`С5` — `.gitignore` без `.DS_Store`; `Д1` — README без секции запуска; `Д3` — 41 файл
одним уровнем в `docs/`; `Д4` — 29 документов-сирот и нет индекса; `К2` — 37 блоков
закомментированного кода; `К3`, `К5`, `К6`, `А1`, `А4`.

## Отрицательный результат, зафиксированный по правилу И6

Первая редакция правил `К1` (пересказ кода) и `К2` (закомментированный код) давала
10 и 7 находок на чистом репозитории `lead_centre`, из них большинство — проза внутри
блочных комментариев. После сужения (`К1` — только однострочный комментарий, начинающийся
с глагола пересказа; `К2` — только однострочный комментарий с терминатором кода)
на `lead_centre` осталось 0 находок по обоим правилам, на `fixtures/dirty` обе проверки
продолжают срабатывать. Граница измерена: семантический пересказ без глагола-маркера
прибор не ловит и не должен — это остаётся человеку.
